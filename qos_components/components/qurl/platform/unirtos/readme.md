[toc]

## Overview

## Build Command

The examples below still use `lzm_linux` as the reference platform. Enter the `build` directory and run:

```shell
# Run cmake
cmake ..
# Build
make
# Run
./qurl_iot.elf
```
