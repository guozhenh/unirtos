[toc]

## Overview

## Build Command

The examples below use `lzm_linux` as the default platform. Enter the `build` directory and run:

```shell
# Run cmake
cmake ..
# Build
make
# Run
./qurl_iot.elf
```
